{
  "name": "msme-marketplace",
  "version": "1.0.0",
  "private": true,
  "description": "MERN marketplace for MSMEs. Tooling root: husky, commitlint, lint-staged.",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.2",
    "@commitlint/config-conventional": "^21.2.2",
    "husky": "^9.1.7",
    "lint-staged": "^17.5.1"
  }
}
